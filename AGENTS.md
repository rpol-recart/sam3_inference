Отлично! Чтобы выполнить качественный рефакторинг FastAPI-проекта с сохранением функциональности и повышением читаемости, поддерживаемости и соответствия лучшим практикам, требуется чёткий и структурированный план. Ниже приведён **подробный план и инструкции для LLM-агента**, который будет выполнять рефакторинг.

---

## 🎯 **Цели рефакторинга**
1. **Сохранить 100% функциональности** — все API-эндпоинты, бизнес-логика, авторизация, обработка ошибок и интеграции должны работать **точно так же**, как до рефакторинга.
2. **Повысить читаемость кода** — устранить “code smells”, упростить структуру.
3. **Улучшить поддерживаемость** — модульность, изолированность компонентов, явные зависимости.
4. **Привести проект в соответствие с best practices FastAPI** и современными стандартами Python (PEP 8, типизация, тестирование и т.д.).

---

## 🧭 **Общий план рефакторинга**

### **Шаг 1: Анализ текущего состояния проекта**
- [ ] Определить структуру проекта (файлы, модули, зависимости).
- [ ] Выявить отсутствие типизации, дублирование кода, глобальные переменные, тесную связность.
- [ ] Проверить наличие и полноту тестов (unit, integration).
- [ ] Убедиться, что CI/CD (если есть) проходит успешно.
- [ ] Составить список “долгов”: анти-паттерны, сложные функции, неявные зависимости.

> 💡 Требование: **до начала рефакторинга должна быть 100% уверенность, что функциональность покрыта тестами или может быть вручную проверена**.

---

### **Шаг 2: Определение целевой архитектуры**
Пример рекомендуемой структуры FastAPI-проекта:

```
project/
├── app/
│   ├── __init__.py
│   ├── main.py                 # точка входа (FastAPI app)
│   ├── core/                   # настройки, безопасность, логирование
│   ├── api/                    # маршруты (routers)
│   │   ├── __init__.py
│   │   ├── deps.py             # зависимости (Depends)
│   │   └── v1/                 # версия API
│   │       ├── __init__.py
│   │       ├── routes/
│   │       └── schemas/
│   ├── models/                 # SQLAlchemy/Pydantic модели
│   ├── schemas/                # Pydantic схемы (если не в api/v1/schemas)
│   ├── services/               # бизнес-логика (domain layer)
│   ├── repositories/           # слой доступа к данным (DAO/репозитории)
│   ├── utils/                  # утилиты
│   └── exceptions/             # кастомные исключения и handlers
├── tests/
│   ├── unit/
│   └── integration/
├── alembic/                    # (если используется миграции)
├── requirements.txt / pyproject.toml
└── README.md
```

> Использовать **архитектуру с чётким разделением слоёв**:  
> **Routes → Services → Repositories → Models**

---

### **Шаг 3: Подробные инструкции для LLM-агента**

> **ВАЖНО**: Каждое изменение должно сопровождаться **проверкой работоспособности** (запуск тестов или ручная проверка эндпоинта).

#### **Инструкция 1: Типизация и документация**
- Добавить **аннотации типов** везде, где отсутствуют.
- Использовать `typing` модуль (`Optional`, `Union`, `List`, `Dict`, `Annotated`).
- Проверить, что все Pydantic-модели используют `BaseModel` и валидацию.
- Добавить **docstrings** к функциям и классам (Google или NumPy стиль).
- Убедиться, что все параметры FastAPI-эндпоинтов имеют аннотации.

#### **Инструкция 2: Разделение ответственности**
- Вынести **бизнес-логику** из роутеров в `services/`.
- Роутеры должны:
  - принимать запрос,
  - вызывать сервис через `Depends`,
  - возвращать ответ.
- Вынести **работу с БД** в `repositories/` (паттерн Repository).
- Не допускать `session` или `engine` в роутерах.

#### **Инструкция 3: Управление зависимостями**
- Все зависимости (DB session, внешние сервисы) должны быть в `api/deps.py`.
- Использовать `Depends()` для внедрения зависимостей.
- Убедиться, что зависимости легко заменяются (для тестов!).

#### **Инструкция 4: Обработка ошибок**
- Создать кастомные исключения в `exceptions/`.
- Написать глобальные exception handlers (в `main.py` или отдельном файле).
- Не использовать `try/except` в роутерах — перенести в сервисы или обработчики.

#### **Инструкция 5: Улучшение читаемости**
- Разбить длинные функции (>30 строк) на более мелкие.
- Устранить вложенные условия (ранний `return`).
- Заменить магические строки/числа на константы (`core/config.py`).
- Использовать понятные имена переменных (`user_id` вместо `uid`).

#### **Инструкция 6: Тесты**
- Убедиться, что каждый сервис и репозиторий покрыт **unit-тестами**.
- Роутеры — **integration-тестами** (с `TestClient`).
- Использовать `pytest` + `pytest-asyncio` (если async).
- Mock внешние зависимости (email, API и т.д.).

#### **Инструкция 7: Конфигурация**
- Вынести настройки в `core/config.py` с использованием `pydantic.BaseSettings`.
- Поддерживать `.env` файлы через `python-dotenv`.

#### **Инструкция 8: Async / Sync**
- Если проект асинхронный — везде использовать `async/await`.
- Не смешивать sync и async (особенно в DB-слое).
- Использовать `async SQLAlchemy 1.4+` или `Tortoise ORM` / `beanie` / `SQLModel`.

#### **Инструкция 9: Безопасность**
- Убедиться, что:
  - Пароли хешируются (`passlib`, `bcrypt`).
  - JWT правильно обрабатывается.
  - CORS настроен корректно.
  - Swagger (`/docs`) не доступен в проде (опционально).

#### **Инструкция 10: Логирование и мониторинг**
- Добавить логирование через `logging` (в `core/logger.py`).
- Логировать вход/выход из сервисов, ошибки.
- Использовать структурированное логирование (`structlog` — опционально).

---

## ✅ **Требования к результату**
- Все существующие тесты проходят **без изменений**.
- Ручная проверка всех критических сценариев успешна.
- Новая структура легко расширяема (добавление новых эндпоинтов, моделей, сервисов).
- Проект соответствует:
  - [FastAPI Best Practices](https://fastapi.tiangolo.com/)
  - [PEP 8](https://peps.python.org/pep-0008/)
  - [SOLID](https://en.wikipedia.org/wiki/SOLID)
  - Чистой архитектуре (Clean Architecture / Hexagonal)

---

## 📌 **Финальная инструкция для LLM-агента**

> «Выполни рефакторинг FastAPI-проекта по шагам выше.  
> **ВАЖНО**:  
> - Не изменяй логику работы — только структуру и стиль.  
> - Сохрани все входные/выходные данные, статусы, заголовки.  
> - После каждого изменения запусти тесты (если есть) или укажи, как проверить вручную.  
> - Предоставляй изменения покомпонентно: сначала структура, затем роутеры, сервисы и т.д.  
> - Используй современный Python (3.10+), типизацию, зависимости через `Depends`.  
> - Убедись, что проект остаётся рабочим на каждом этапе.»
